import 'package:flutter/material.dart';

class AppColors {
  // DARK THEME COLORS
  static const Color background = Color(0xFF0D0D0F); // almost black
  static const Color card = Color(0xFF18181C);       // dark grey card
  static const Color gold = Color(0xFFFFC248);       // gold accent

  // Text colors
  static const Color textLight = Colors.white;       // white
  static const Color textGrey = Color(0xFF9E9E9E);   // grey text

  // Additional optional colors (not required but useful)
  static const Color success = Colors.green;
  static const Color error = Colors.redAccent;

  // Light theme placeholder (we will configure later)
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color lightCard = Colors.white;
}
